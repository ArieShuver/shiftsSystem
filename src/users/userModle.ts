export class User {
    id: number;
    name: string;
    password: string;
    role: string;

    constructor(id: number, name:string, password:string, role:string) {
        id = id;
        name = name;
        password = password;
        role = role;
    }
}
